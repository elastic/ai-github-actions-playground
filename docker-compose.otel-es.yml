# Adds a local single-node Elasticsearch to the OTel stack.
# Usage: docker compose -f docker-compose.otel.yml -f docker-compose.otel-es.yml up -d
services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:9.0.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - xpack.license.self_generated.type=trial
      - ES_JAVA_OPTS=-Xms1g -Xmx1g
    ports:
      - "9200:9200"
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200 >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s

  otel-collector:
    environment:
      - ES_URL=http://elasticsearch:9200
      - ES_API_KEY=
    depends_on:
      elasticsearch:
        condition: service_healthy
